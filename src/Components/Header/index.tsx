import React from 'react';
import {Container} from './styles';

const Index: React.FC = () => {
      return (
            <Container>
            <div> </div>
            </Container>
      );
    }

export default Index;